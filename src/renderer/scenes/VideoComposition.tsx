// AUTO-GENERATED by Kinetic Multi-Agent Pipeline — DO NOT EDIT MANUALLY
import React from 'react';
import { Series, useCurrentFrame } from 'remotion';
import { AppCanvas, BrowserFrame, MockWindow, SidebarLayout } from '../primitives/StructuralSDK';
import { BarChartCard } from '../primitives/ChartsSDK';
import { FadeBlur, PulseScale, SlideInOut } from '../primitives/TransitionSDK';

const Scene1: React.FC = () => {
  const primaryColor = '#F7B928';
  const secondaryColor = '#a78bfa';
  const accentColor = '#8BC34A';
  const semanticColor = '#3b82f6';
  const errorColor = '#ef4444';
  const successColor = '#22c55e';
  const neutralColor = '#64748b';
  const surfaceColor = '#2E2E2E';
  const backgroundColor = '#1A1A1A';
  const textColor = '#D9D9D9';
  const fontFamily = 'Inter, Inter, sans-serif';

  return (
    <div
      style={{
        width: '100%',
        height: '100%',
        position: 'relative',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: backgroundColor,
        fontFamily: fontFamily,
        overflow: 'hidden',
      }}
    >
      <FadeBlur duration={25} startBlur={12}>
        <MockWindow
          visible={true}
          width={1200}
          height={800}
          isFrameless={true}
          windowColor={surfaceColor}
          headerColor={primaryColor}
          controlsPosition="right"
          glowConfig={{ enabled: true, color: primaryColor, intensity: 8, spread: 4 }}
          rotateX={10}
          rotateY={10}
          perspective={1200}
          translateZ={50}
        >
          <div style={{
            position: 'absolute',
            top: 0,
            left: 0,
            width: '100%',
            height: '100%',
            backgroundColor: backgroundColor,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
          }}>
            <div style={{
              backgroundColor: surfaceColor,
              padding: 20,
              borderRadius: 10,
              boxShadow: '0 0 10px rgba(0, 0, 0, 0.2)',
              width: 400,
              height: 200,
            }}>
              <h1 style={{
                fontFamily: fontFamily,
                fontSize: 24,
                color: textColor,
                marginBottom: 10,
              }}>
                GuardRail Cloud Security
              </h1>
              <p style={{
                fontFamily: fontFamily,
                fontSize: 18,
                color: textColor,
                marginBottom: 20,
              }}>
                Protect your cloud infrastructure with our robust security solutions.
              </p>
              <div style={{
                display: 'flex',
                justifyContent: 'space-between',
                alignItems: 'center',
              }}>
                <div style={{
                  backgroundColor: surfaceColor,
                  padding: 5,
                  borderRadius: 5,
                  color: primaryColor,
                  fontSize: 14,
                  marginRight: 10,
                }}>
                  <span style={{
                    fontFamily: fontFamily,
                    fontSize: 14,
                    color: primaryColor,
                  }}>
                    99.99% Uptime
                  </span>
                </div>
                <div style={{
                  backgroundColor: surfaceColor,
                  padding: 5,
                  borderRadius: 5,
                  color: primaryColor,
                  fontSize: 14,
                  marginLeft: 10,
                }}>
                  <span style={{
                    fontFamily: fontFamily,
                    fontSize: 14,
                    color: primaryColor,
                  }}>
                    100% Compliance
                  </span>
                </div>
              </div>
            </div>
          </div>
        </MockWindow>
      </FadeBlur>
    </div>
  );
};

const Scene2: React.FC = () => {
  const primaryColor = '#F7B928';
  const secondaryColor = '#a78bfa';
  const accentColor = '#8BC34A';
  const semanticColor = '#3b82f6';
  const errorColor = '#ef4444';
  const successColor = '#22c55e';
  const neutralColor = '#64748b';
  const surfaceColor = '#2E2E2E';
  const backgroundColor = '#1A1A1A';
  const textColor = '#D9D9D9';
  const fontFamily = 'Inter, Inter, sans-serif';

  return (
    <div
      style={{
        width: '100%',
        height: '100%',
        position: 'relative',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: backgroundColor,
        fontFamily: fontFamily,
        overflow: 'hidden',
      }}
    >
      <SlideInOut direction="bottom" distance={80} duration={24} fade={true}>
        <AppCanvas
          width={1920}
          height={1080}
          backgroundColor={backgroundColor}
          gridPattern={true}
          gridColor={neutralColor}
          gridSpacing={20}
          glowConfig={{ enabled: true, color: primaryColor, intensity: 8, spread: 4 }}
          perspective={1200}
          rotateX={10}
          rotateY={10}
        >
          <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100%' }}>
            <BarChartCard
              title="Real-Time Threat Detections"
              categories={['SQLi', 'XSS', 'DDoS', 'Brute Force']}
              values={[120, 85, 40, 15]}
              barColor={primaryColor}
              width={600}
              height={400}
              glowConfig={{ enabled: true, color: primaryColor, intensity: 8, spread: 4 }}
            />
          </div>
        </AppCanvas>
      </SlideInOut>
    </div>
  );
};

const Scene3: React.FC = () => {
  const primaryColor = '#F7B928';
  const secondaryColor = '#a78bfa';
  const accentColor = '#8BC34A';
  const semanticColor = '#3b82f6';
  const errorColor = '#ef4444';
  const successColor = '#22c55e';
  const neutralColor = '#64748b';
  const surfaceColor = '#2E2E2E';
  const backgroundColor = '#1A1A1A';
  const textColor = '#D9D9D9';
  const fontFamily = 'Inter, Inter, sans-serif';

  return (
    <div
      style={{
        width: '100%',
        height: '100%',
        position: 'relative',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: backgroundColor,
        fontFamily: fontFamily,
        overflow: 'hidden',
      }}
    >
      <PulseScale cycleFrames={90} minScale={0.97} maxScale={1.03}>
        <BrowserFrame
          width="100%"
          height="100%"
          backgroundColor={backgroundColor}
          borderRadius={0}
          showNavArrow={false}
          showNewTabButton={false}
          glowConfig={{ enabled: true, color: primaryColor, intensity: 8, spread: 4 }}
          rotateX={5}
          rotateY={5}
          perspective={1200}
        >
          <div
            style={{
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              height: '100%',
              width: '100%',
            }}
          >
            <h1 style={{ fontSize: 48, fontFamily, color: textColor, margin: 0, padding: 0 }}>
              Cluster Security Dashboard
            </h1>
            <p style={{ fontSize: 24, fontFamily, color: textColor, margin: 0, padding: 0, maxWidth: 600 }}>
              Monitor and analyze security threats in real-time.
            </p>
            <div style={{ display: 'flex', flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', width: '100%', padding: 20 }}>
              <div style={{ backgroundColor: surfaceColor, padding: 10, borderRadius: 10, display: 'flex', flexDirection: 'row', alignItems: 'center', justifyContent: 'center' }}>
                <span style={{ fontSize: 18, fontFamily, color: successColor, margin: 0, padding: 0 }}>95%</span>
                <span style={{ fontSize: 14, fontFamily, color: textColor, margin: 0, padding: 0, marginLeft: 5 }}>Threats contained</span>
              </div>
              <div style={{ backgroundColor: surfaceColor, padding: 10, borderRadius: 10, display: 'flex', flexDirection: 'row', alignItems: 'center', justifyContent: 'center' }}>
                <span style={{ fontSize: 18, fontFamily, color: errorColor, margin: 0, padding: 0 }}>5%</span>
                <span style={{ fontSize: 14, fontFamily, color: textColor, margin: 0, padding: 0, marginLeft: 5 }}>Threats detected</span>
              </div>
            </div>
          </div>
        </BrowserFrame>
      </PulseScale>
    </div>
  );
};

export const VideoComposition: React.FC<{ bgSelection?: any }> = ({ bgSelection }) => {
  const bgColor = bgSelection?.color || '#1A1A1A';
  const bgType = bgSelection?.type || 'color';

  let backdropStyle: React.CSSProperties = { backgroundColor: bgColor };
  if (bgType === 'gradient' && bgSelection?.gradient) {
    backdropStyle = { background: bgSelection.gradient };
  } else if (bgType === 'image' && bgSelection?.imageUrl) {
    backdropStyle = { backgroundImage: `url(${bgSelection.imageUrl})`, backgroundSize: 'cover', backgroundPosition: 'center' };
  }

  return (
    <div className="w-full h-full text-white relative overflow-hidden">
      <div style={backdropStyle} className="absolute inset-0 pointer-events-none z-0" />
      <div className="relative z-10 w-full h-full">
        <Series>
          <Series.Sequence durationInFrames={120}>
            <Scene1 />
          </Series.Sequence>
          <Series.Sequence durationInFrames={150}>
            <Scene2 />
          </Series.Sequence>
          <Series.Sequence durationInFrames={180}>
            <Scene3 />
          </Series.Sequence>
        </Series>
      </div>
    </div>
  );
};

export default VideoComposition;